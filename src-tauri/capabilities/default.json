{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the myterm window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
